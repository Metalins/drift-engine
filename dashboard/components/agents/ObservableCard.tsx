/**
 * ObservableCard — TOMBSTONED Sprint 5 (2026-05-14).
 *
 * Originally rendered one Trinity observable tile (ICR / TWC / TTM /
 * β-Crooks). The pivot to closed algorithm removed these from
 * `/agents/[id]`. The barrel (`./index.ts`) no longer re-exports this
 * module. Kept as a tombstone so the git history points at one
 * canonical removal commit instead of a deleted file.
 *
 * `git log -- components/agents/ObservableCard.tsx` shows the original
 * implementation.
 */
export {};
